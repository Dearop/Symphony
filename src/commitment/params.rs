//! Parameter generation and MSIS matrix sampling for commitments.
//!
//! Note: `AjtaiParams::setup` is used directly throughout the crate.
//! Structured MSIS matrices for two-layer folding are built in `folding::two_layer`.
